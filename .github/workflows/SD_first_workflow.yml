name: SD My first workflow
on:
  push:
    branches: [main]
    paths-ignore: [.github/**]
  schedule:
    - cron: "1,31 8-18 * * 1-5"
  workflow_dispatch:

jobs:
  First-Job:
    runs-on: ubuntu-latest
    permissions: read-all
    steps:
      - run: |
        echo "This job was triggered by ${{ githib.event_name }}"
        echo "Brnach name is ${{ github.ref }} and repoasitory is ${{github.repository}}."
        echo "Trigger: ${{github.actor}} using ${{ github.event_name }}>> $GITHUB_STEP_SUMMARY
        
        
  
